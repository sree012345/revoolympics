# Player app — per-environment Firebase notes

| Directory | Project ID |
|-----------|------------|
| `development/` | `revoolympics-dev` |
| `staging/` | `revoolympics-staging` |
| `production/` | `revoolympics-prod` |

Store environment-specific registration notes here. Native config files live under `android/` and `ios/` (see `firebase/README.md` in this app).
